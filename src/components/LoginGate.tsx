import { useState, type FormEvent } from 'react'
import { useAccount } from '../context/AccountContext'
import { ProductMark } from './VisualSystem'
import '../styles/account-gate.css'
export default function LoginGate() { const { login, continueAsNormal } = useAccount(); const [password, setPassword] = useState(''), [busy, setBusy] = useState(false), [error, setError] = useState(''); const submit = async (event: FormEvent) => { event.preventDefault(); if (busy) return; setBusy(true); setError(''); const ok = await login(password); setBusy(false); if (!ok) setError('Incorrect premium password or Premium access is temporarily unavailable.') }; return <main className="account-gate"><section><div className="account-gate__brand"><ProductMark /><strong>NUTSCOPE</strong></div><p className="eyebrow">ACCOUNT ACCESS</p><h1>Choose your access</h1><p className="intro">Explore the Thetanuts options experience at your own pace.</p><div className="account-gate__choices"><article><span>FREE</span><h2>Normal access</h2><p>Explore markets, analyze options, use Strategy Lab paper trading, and access existing standard functionality.</p><button type="button" onClick={continueAsNormal}>Continue as Free</button></article><article><span>PREMIUM</span><h2>Premium access</h2><p>Premium access for advanced features as they are added.</p><form onSubmit={submit}><input type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Premium password" aria-label="Premium password" required /><button disabled={busy}>{busy ? 'Unlocking…' : 'Unlock Premium'}</button></form>{error && <p role="alert" className="account-gate__error">{error}</p>}</article></div></section></main> }
