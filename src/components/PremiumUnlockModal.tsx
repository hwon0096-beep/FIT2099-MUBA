import { useState } from 'react'
import { useAccount } from '../context/AccountContext'
export default function PremiumUnlockModal({ onClose }: { onClose: () => void }) { const { login } = useAccount(); const [password, setPassword] = useState(''), [error, setError] = useState(''), [busy, setBusy] = useState(false); const unlock = async () => { setBusy(true); setError(''); if (await login(password)) onClose(); else setError('Unable to unlock Premium with that password.'); setBusy(false) }; return <div className="modal-backdrop"><section className="modal-panel"><h2>Unlock Advanced Strategies</h2><p className="modal-subtext">Premium includes multi-leg paper strategies, advanced payoff analysis, comparison, and scenario tools.</p><input className="premium-password" type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Premium password" /><div className="modal-footer"><button type="button" className="modal-cancel" onClick={onClose}>Maybe later</button><button type="button" className="modal-primary" disabled={busy} onClick={() => void unlock()}>{busy ? 'Unlocking…' : 'Unlock Premium'}</button></div>{error && <p className="pnl-negative">{error}</p>}</section></div> }
